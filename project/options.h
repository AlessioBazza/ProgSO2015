#ifndef SISOP_PROJ_OPTIONS_H
#define SISOP_PROJ_OPTIONS_H

void funcGetParams(int, char**, char*, char*, char*, char*, char*);

#endif

/*
funcGetParams
prende in input argc, argv, la stringa prompt, l'indirizzo di loglevel, la stringa di logfile, l'indirizzo di logstatus (log attivo/disattivo) e la stringa di logstatustext
(int, char**, char*, char*, char*, char*, char* -> void)
*/
